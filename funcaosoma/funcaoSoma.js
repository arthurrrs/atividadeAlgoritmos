function calcular Media(array) {
    if (array.length === 0) {
        return 0;
    }
}

const soma = array.reduce((acc,num) => acc = num, 0);
const media = soma / array.length;

return media; {
    
}

