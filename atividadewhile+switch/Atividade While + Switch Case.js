    //1    O laço while começa avaliando a condição . Se condition for true , o código dentro do bloco será executado. Se a condição for false , o código dentro do bloco não será executado e o laço se encerra.

    //2     Da mesma forma que o comando if, o comando switch case controla o fluxo do programa permitindo ao programador especificar código diferente para ser executado em várias condições. Em particular, um comando switch compara o valor de uma variável aos valores especificados nos comandos case .

    //3     Voce pode agrupar os casos usando o mesmo bloco de codigo para cada um deles, isso pode ser util quando voce deseja executar a mesma logica para diferentes valores de caso.

    //4     Case é a condicional a ser satisfeita, se verdadeiro o código em seu interior será executado. No exemplo dado, será impresso na tela “Outubro”. break é uma declaração de término de execução do switch, sua declaração é opcional. default é uma declaração padrão para caso não satisfaça nenhuma das condições anteriores.

    //5     Nao, as expressoes booleanas nao sao suportadas diferamente nos casos do switch-case em muitas linguagens de programação, incluindo javascript.

    //6     Sim, é possivel ter um caso padrao (default case) em um bloco switch-case em varias linguagens de programacao, incluindo javascript. O caso padrao é usado quando nenhum dos casos correspondentes é encontrado.

    //7     Se nao houver nenhum caso correspondente ao valor fornecido no switch-case, o bloco de codigo do caso padrao(default case) sera executado.

    //8     Sim, é possivel usar switch-case sem um loop.

    //9     Sim, é possivel ter casos vazios no switch-case, um cado vazio ocorre quando nao ha codigo especifico a ser executado para um determinado caso, nesse caso o controle simplesmente passa para o proximo caso.

